# git ls-files --others --exclude-from=.git/info/exclude
# Lines that start with '#' are comments.
# For a project mostly in C, the following would be a good set of
# exclude patterns (uncomment them if you want to use them):
# *.[oa]
# *~

# Blacklist all directories, then whitelist all hidden directories
*/
!.*/

# Blacklist OS-related files and directories
.CFUserTextEncoding
.cups/
.DS_Store
.fontconfig/
.Trash/

# Blacklist hidden files and directories that pose a security risk
.*history
.lesshst
.ssh/
.subversion/
.viminfo

# Whitelist subversion config
!.subversion/config
